apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: postgres-data
    namespace: gfp
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: local-path
    resources:
        requests:
            storage: 5Gi
